# Parsed Excel Schema (attachments/20250925_100507_Dashboard_Data_Fields.xlsx)

## Sheets
- Sheet1

## Dashboard: Referral dashboard
### Collection: users

| Field Path | Type | Description |
| --- | --- | --- |
| referral_code | string | Used to onboard users through referrals. |
| referral_stats | array<object> | List of objects containing referral statistics. |
| referral_stats.total_referrals | integer | Number of users onboarded based on the referral code |
| referral_stats.verified_referrals | integer | Number of users who have been verified. |
| referral_stats.last_referral_date | date | The most recent date a referral occurred. |
| referral_history | array<object> | List of referral history objects containing detailed referral data. |
| referral_history.user_id | string | Unique identifier of the referred user. |
| referral_history.user_email | string | Email address of the referred user. |
| referral_history.user_name | string | Name of the referred user. |
| referral_history.referred_at | datetime | Timestamp when the referral occurred. |
| referral_history.verified_at | datetime | Timestamp when the referral was verified. |
| referral_history.status | string | Status of the referral (e.g., pending, verified) |

## Dashboard: Session dashboard
### Collection: session_tracking

| Field Path | Type | Description |
| --- | --- | --- |
| task_id | string | Has the value of the particular task which is present. |
| tenant_id | string | The organization this session belongs to. |
| organization_name | string | Name of the organization. |
| user_id | string | Unique identifier of the user. |
| User_name | string | Name of the user who performs the session |
| project_id | string | The project ID associated with the session. |
| container_id | string | Value indicating if the session belongs to code generation or code maintenance. |
| service_type | string | Type of service, e.g., code generation, code query, deep query, interactive configuration, auto configuration, or code maintenance. |
| session_start | datetime | Timestamp when the session started. |
| session_end | datetime | Timestamp when the session ended. |
| status | string | Status of the session (active, completed, failed). |
| total_cost | number | Sum of all agent costs. |
| agent_costs | object | Object containing a list of agents and their respective costs |
| cost_history | array<object> | Array of objects representing various agent costs and total costs for particular durations. |
| cost_history.timestamp | datetime | Timestamp for this cost entry. |
| cost_history.agent_costs | object | Costs for each agent at this timestamp. |
| cost_history.total_cost | number | Total cost for this timestamp. |
| last_updated | datetime | Timestamp of the last update for this session. |
| session_data | object | Object containing session-specific data. |
| session_data.llm_model | string | Name of the LLM model currently used in this session. |
| session_data.session_name | string | Name of the particular session |
| session_data.description | string | Description for the particular session |
| session_data.platform | string | which platform is been used to perform the operation for ex : web |
| session_data.selected_repos | object | object containing the list of repositories used in code maintenance |
| session_data.selected_repos.all_repositories | boolean | Boolean: true if all repositories are selected, false if specific repositories are selected |
| session_data.selected_repos.repositories | array<string> | Array containing IDs of selected repositories if all_repositories is false. |
| created_at | datetime | Timestamp when the session was created. |

## Dashboard: API dashboard
### Collection: request_tracker

| Field Path | Type | Description |
| --- | --- | --- |
| request_id | string | Unique identifier for the API request. |
| created_at | datetime | Timestamp when the request was created. |
| date_partition | date | Extracts the date (YYYY-MM-DD) from a datetime to organize or segment data by day. |
| hour_partition | integer | Extracts the hour (0–23) from a datetime to organize or segment data by hour. |
| tenant_id | string | Organization ID. |
| tenant_name | string | Name of the organization |
| user_id | string | Unique identifier of the user making the request. |
| user_email | string | Email of the user. |
| username | string | Name of the user. |
| user_role | string | Role of the user (e.g., admin or user). |
| method | string | HTTP method used (e.g., GET). |
| endpoint | string | API endpoint called. |
| full_url | string | Complete URL of the request. |
| status_code | integer | HTTP response status code. |
| duration_ms | integer | Duration of the request in milliseconds. |
| response_size_bytes | integer | Content-Length from response headers, defaulting to 0 if missing. |
| response_type | string | Type of response returned (e.g., success). |
| client_ip | string | IP address of the client |
| user_agent | string | Browser or client user agent string. |
| device_type | string | Device type (e.g., desktop). |
| browser | string | Browser name (e.g., Chrome). |
| country | string | Country of the client. |
| region | string | Region of the client. |
| auth_method | string | Authentication method (e.g., Bearer). |
| has_valid_token | boolean | Boolean: true if the token is valid, false otherwise. |
| project_id | string | ID of the project related to the request. |
| resource_type | string | Type of resource (e.g., users, projects, tasks). |
| action_type | string | Determine the action type based on HTTP method and path |
| custom_headers | object | Object containing custom header values. |
| custom_headers.is_public_selected | boolean | Boolean: public or private project. |
| custom_headers.selected_tenant_id | string | Organization ID from the header. |
| custom_headers.bypass_credit_check | string | Value from "Bypass-Credit-Check" header, default empty string if not present |
| query_params | object | Object containing all query parameter keys and their values. |
| tags | array<string> | Array of tags categorizing requests by method, response status, resource type, and user role. |

## Dashboard: Deployment dashboard
### Collection: app_deployments

| Field Path | Type | Description |
| --- | --- | --- |
| app_id | string | Unique identifier for the deployed application. |
| app_url | string | Publicly accessible URL of the deployed application |
| artifact_path | string | Storage path or location of the deployment artifacts (e.g., build files, images). |
| branch_name | string | The Git branch from which the deployment was created. |
| build_path | string | Path to the build directory containing compiled application files. |
| command | string | Build command executed for deployment (e.g., npm run build). |
| created_at | datetime | Timestamp when the deployment was created. |
| custom_domain | string | Custom domain mapped to the deployed application, if configured. |
| deployment_id | string | Unique identifier for the deployment instance. |
| job_id | string | Identifier of the deployment job or process execution. |
| message | string | Additional message or notes related to the deployment |
| project_id | string | project id associated with the deployment. |
| project_name | string | Name of the project associated with the deployment. |
| root_path | string | Root directory path from which the deployment files are served. |
| status | string | Current status of the deployment (e.g., success, failed, in-progress). |
| subdomain | string | Auto-generated subdomain assigned to the deployment. |
| task_id | string | Particular task where the deployment happend for the project |
| tenant_id | string | Organization id associated with the deployment. |
| tenant_name | string | Organization name associated with the deployment. |
| updated_at | datetime | Timestamp of the most recent update to the deployment. |
| artifact_count | integer | Number of artifacts generated or uploaded during deployment. |
| domain_status | string | Status of the custom domain (e.g., verified, pending, failed). |
| domain_checked_at | datetime | Timestamp when the custom domain was last verified or checked. |
